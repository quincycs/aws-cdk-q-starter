{
  "name": "cdk-q-starter",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && npm run bootstrap && cdk deploy --require-approval=never --all",
    "bootstrap": "cdk bootstrap",
    "synth": "npm run build && cdk synthesize --all",
    "diff": "npm run build && cdk diff --all"
  },
  "author": {
    "name": "Quincy Mitchell",
    "url": "http://quincymitchell.com",
    "organization": false
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^8.10.38",
    "typescript": "^4.1.3",
    "aws-cdk": "2.15.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.10.0",
    "constructs": "^10.0.0"
  }
}
